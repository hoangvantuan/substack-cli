# substack-cli

Instructions for this repo live in `AGENTS.md` so every agent reads the same file.

@AGENTS.md

> Maintainers: edit `AGENTS.md`, not this file. The `## Agent skills` block lives there.
